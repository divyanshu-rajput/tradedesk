import { EnvironmentProviders } from '@angular/core';

/** Firebase providers wired in Phase 5 after environment config is pasted. */
export function provideFirebaseProviders(): EnvironmentProviders[] {
  return [];
}
